import { Record } from './types/types';
export declare function getMonthFilter(month: number): (record: Record) => boolean;
export declare function getYearFilter(year: number): (record: Record) => boolean;
